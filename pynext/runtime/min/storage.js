(function(global){'use strict';const storageSignals=new Map();function useStorage(id,key,defaultValue,storageType='local'){const storage=storageType==='session' ? sessionStorage:localStorage;let value;try{const stored=storage.getItem(key);value=stored!==null ? JSON.parse(stored):defaultValue;}catch(e){value=defaultValue;}const subscribers=new Set();const signal={id,key,storageType,read:()=>value,write:(newValue)=>{if(value!==newValue){value=newValue;try{storage.setItem(key,JSON.stringify(newValue));}catch(e){console.warn(`Failed to persist ${key}:`,e);}subscribers.forEach(fn=>fn(newValue));updateStorageDOM(id,newValue);}},update:(fn)=>{signal.write(fn(value));},subscribe:(fn)=>{subscribers.add(fn);return()=>subscribers.delete(fn);},};storageSignals.set(id,signal);if(window.__pynext__?.signals){window.__pynext__.signals[id]=signal;}log(`Created storage signal:${key}(${storageType})`);return signal;}function updateStorageDOM(signalId,value){const elements=document.querySelectorAll(`[data-storage-signal="${signalId}"]`);elements.forEach(el=>{const bindType=el.dataset.storageBind||'text';switch(bindType){case 'text':el.textContent=String(value);break;case 'value':el.value=value;break;case 'class':el.className=String(value);break;}});}function getStorageSignal(id){return storageSignals.get(id)||null;}function setupCrossTabSync(){window.addEventListener('storage',(event)=>{for(const[id,signal]of storageSignals){if(signal.key===event.key&&signal.storageType==='local'){try{const newValue=JSON.parse(event.newValue);signal._value=newValue;if(signal._subscribers){signal._subscribers.forEach(fn=>fn(newValue));}updateStorageDOM(id,newValue);log(`Cross-tab sync:${signal.key}=${newValue}`);}catch(e){console.warn(`Failed to sync ${signal.key}:`,e);}break;}}});}function get(key,defaultValue=null,storageType='local'){const storage=storageType==='session' ? sessionStorage:localStorage;try{const stored=storage.getItem(key);return stored!==null ? JSON.parse(stored):defaultValue;}catch(e){return defaultValue;}}function set(key,value,storageType='local'){const storage=storageType==='session' ? sessionStorage:localStorage;try{storage.setItem(key,JSON.stringify(value));}catch(e){console.warn(`Failed to set ${key}:`,e);}}function remove(key,storageType='local'){const storage=storageType==='session' ? sessionStorage:localStorage;storage.removeItem(key);}const DEBUG=typeof window!=='undefined'&&window.__PYNEXT_DEBUG__;function log(...args){if(DEBUG){}}function init(){setupCrossTabSync();log('Storage runtime initialized');}function hydrate(storageConfigs){if(!storageConfigs)return;storageConfigs.forEach(config=>{useStorage(config.id,config.key,config.default,config.storageType);});log(`Hydrated ${storageConfigs.length}storage signals`);}if(typeof window!=='undefined'){window.__pynext__=window.__pynext__||{};window.__pynext__.storage={useStorage,getStorageSignal,get,set,remove,hydrate,};window.__pynext__.useStorage=useStorage;if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init);}else{init();}}if(typeof module!=='undefined'&&module.exports){module.exports={useStorage,getStorageSignal,get,set,remove,hydrate,};}})(typeof window!=='undefined' ? window:global);
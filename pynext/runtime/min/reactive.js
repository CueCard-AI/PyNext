let currentObserver=null;let batchDepth=0;const pendingEffects=new Set();export function createSignal(initialValue,options={}){let value=initialValue;const subscribers=new Set();const equals=options.equals||((a,b)=>a===b);function signal(){if(currentObserver){subscribers.add(currentObserver);}return value;}signal.set=function(newValue){if(equals(value,newValue)){return;}value=newValue;notify();};signal.update=function(fn){signal.set(fn(value));};signal.peek=function(){return value;};function notify(){for(const subscriber of subscribers){if(batchDepth>0){pendingEffects.add(subscriber);}else{subscriber.execute();}}}signal._unsubscribe=function(subscriber){subscribers.delete(subscriber);};return signal;}export function createEffect(fn){let cleanup=null;let disposed=false;const effect={execute:function(){if(disposed)return;if(cleanup){cleanup();cleanup=null;}const prevObserver=currentObserver;currentObserver=effect;try{const result=fn();if(typeof result==='function'){cleanup=result;}}finally{currentObserver=prevObserver;}}};effect.execute();return function dispose(){disposed=true;if(cleanup){cleanup();cleanup=null;}};}export function createMemo(fn,options={}){let value;let dirty=true;const subscribers=new Set();const equals=options.equals||((a,b)=>a===b);const computation={execute:function(){dirty=true;for(const subscriber of subscribers){if(batchDepth>0){pendingEffects.add(subscriber);}else{subscriber.execute();}}}};function memo(){if(currentObserver){subscribers.add(currentObserver);}if(dirty){const prevObserver=currentObserver;currentObserver=computation;try{const newValue=fn();if(!equals(value,newValue)){value=newValue;}}finally{currentObserver=prevObserver;}dirty=false;}return value;}memo.peek=function(){if(dirty){const prevObserver=currentObserver;currentObserver=null;try{value=fn();}finally{currentObserver=prevObserver;}dirty=false;}return value;};return memo;}export function createStore(initialValue){const subscribers=new Set();function notify(){for(const subscriber of subscribers){if(batchDepth>0){pendingEffects.add(subscriber);}else{subscriber.execute();}}}function createProxy(target,path=[]){if(target===null||typeof target!=='object'){return target;}return new Proxy(target,{get(obj,prop){if(prop==='__isProxy')return true;if(prop==='__target')return obj;if(prop==='__path')return path;if(currentObserver&&typeof prop!=='symbol'){subscribers.add(currentObserver);}const value=obj[prop];if(Array.isArray(obj)&&typeof value==='function'){const mutatingMethods=['push','pop','shift','unshift','splice','sort','reverse','fill','copyWithin'];if(mutatingMethods.includes(prop)){return function(...args){const result=Array.prototype[prop].apply(obj,args);notify();return result;};}}if(value!==null&&typeof value==='object'&&!value.__isProxy){return createProxy(value,[...path,prop]);}return value;},set(obj,prop,value){if(obj[prop]!==value){obj[prop]=value;notify();}return true;},deleteProperty(obj,prop){if(prop in obj){delete obj[prop];notify();}return true;}});}return createProxy(initialValue);}export function batch(fn){batchDepth++;try{fn();}finally{batchDepth--;if(batchDepth===0){const effects=[...pendingEffects];pendingEffects.clear();for(const effect of effects){effect.execute();}}}}export function untrack(fn){const prevObserver=currentObserver;currentObserver=null;try{return fn();}finally{currentObserver=prevObserver;}}export function Show({when,children,fallback,parent}){if(!parent)return{dispose:()=>{}};const marker=document.createComment('show');parent.appendChild(marker);let currentNodes=[];let currentBranch=null;const dispose=createEffect(()=>{const condition=when();const newBranch=condition ? 'children':'fallback';if(newBranch===currentBranch)return;for(const node of currentNodes){node.remove();}currentNodes=[];const render=condition ? children:fallback;if(render){const content=render();const nodes=normalizeNodes(content);for(const node of nodes){parent.insertBefore(node,marker);}currentNodes=nodes;}currentBranch=newBranch;});return{dispose};}export function For({each,key:keyFn,children,fallback,parent}){if(!parent)return{dispose:()=>{}};const marker=document.createComment('for');parent.appendChild(marker);const nodeMap=new Map();let currentNodes=[];const dispose=createEffect(()=>{const items=each()||[];if(items.length===0){for(const node of currentNodes){node.remove();}currentNodes=[];nodeMap.clear();if(fallback){const content=fallback();const nodes=normalizeNodes(content);for(const node of nodes){parent.insertBefore(node,marker);}currentNodes=nodes;}return;}if(currentNodes.length>0&&nodeMap.size===0){for(const node of currentNodes){node.remove();}currentNodes=[];}const newKeys=new Set();const newNodes=[];for(let i=0;i<items.length;i++){const item=items[i];const itemKey=keyFn ? keyFn(item):i;newKeys.add(itemKey);let entry=nodeMap.get(itemKey);if(!entry){const content=children(item,()=>i);const nodes=normalizeNodes(content);entry={nodes,item,key:itemKey};nodeMap.set(itemKey,entry);}newNodes.push(...entry.nodes);}for(const[key,entry]of nodeMap){if(!newKeys.has(key)){for(const node of entry.nodes){node.remove();}nodeMap.delete(key);}}for(const node of newNodes){parent.insertBefore(node,marker);}currentNodes=newNodes;});return{dispose};}export function Index({each,children,fallback,parent}){return For({each,key:(_,i)=>i,children,fallback,parent});}export function Switch({children:matches,parent}){if(!parent||!matches)return{dispose:()=>{}};const marker=document.createComment('switch');parent.appendChild(marker);let currentNodes=[];let currentIndex=-1;const dispose=createEffect(()=>{let matchIndex=-1;let matchRender=null;for(let i=0;i<matches.length;i++){const match=matches[i];const condition=typeof match.when==='function' ? match.when():match.when;if(condition){matchIndex=i;matchRender=match.children;break;}}if(matchIndex===currentIndex)return;for(const node of currentNodes){node.remove();}currentNodes=[];if(matchRender){const content=matchRender();const nodes=normalizeNodes(content);for(const node of nodes){parent.insertBefore(node,marker);}currentNodes=nodes;}currentIndex=matchIndex;});return{dispose};}export function Match({when,children}){return{when,children};}export function Portal({mount,children}){const target=typeof mount==='string'
? document.querySelector(mount):mount;if(!target)return{dispose:()=>{}};const container=document.createElement('div');container.setAttribute('data-portal','');target.appendChild(container);let nodes=[];const dispose=createEffect(()=>{container.innerHTML='';if(children){const content=children();nodes=normalizeNodes(content);for(const node of nodes){container.appendChild(node);}}});return{dispose:()=>{dispose();container.remove();}};}export function ErrorBoundary({fallback,children,parent}){if(!parent)return{dispose:()=>{}};const marker=document.createComment('error-boundary');parent.appendChild(marker);let currentNodes=[];const dispose=createEffect(()=>{for(const node of currentNodes){node.remove();}currentNodes=[];try{const content=children();const nodes=normalizeNodes(content);for(const node of nodes){parent.insertBefore(node,marker);}currentNodes=nodes;}catch(error){if(fallback){const content=fallback(error);const nodes=normalizeNodes(content);for(const node of nodes){parent.insertBefore(node,marker);}currentNodes=nodes;}}});return{dispose};}export function hydrate(root=document.body){const stateScript=document.getElementById('__PYNEXT_DATA__');if(!stateScript)return;let state;try{state=JSON.parse(stateScript.textContent);}catch(e){console.error('[PyNext] Failed to parse hydration state:',e);return;}const components=root.querySelectorAll('[data-pynext-component]');for(const el of components){const id=el.getAttribute('data-pynext-id');const componentState=state.components?.[id];if(componentState){hydrateComponent(el,componentState);}}}export function hydrateIsland(selector){const el=typeof selector==='string'
? document.querySelector(selector):selector;if(!el)return;const stateScript=document.getElementById('__PYNEXT_DATA__');if(!stateScript)return;let state;try{state=JSON.parse(stateScript.textContent);}catch(e){console.error('[PyNext] Failed to parse hydration state:',e);return;}const id=el.getAttribute('data-pynext-id');const componentState=state.components?.[id];if(componentState){hydrateComponent(el,componentState);}}function hydrateComponent(el,state){const signals={};if(state.signals){for(const[name,value]of Object.entries(state.signals)){signals[name]=createSignal(value);}}const stores={};if(state.stores){for(const[name,value]of Object.entries(state.stores)){stores[name]=createStore(value);}}const textBindings=el.querySelectorAll('[data-pynext-text]');for(const textEl of textBindings){const signalName=textEl.getAttribute('data-pynext-text');const signal=signals[signalName];if(signal){createEffect(()=>{textEl.textContent=String(signal());});}}const attrElements=el.querySelectorAll('[data-pynext-attr]');for(const attrEl of attrElements){const attrSpec=attrEl.getAttribute('data-pynext-attr');const bindings=attrSpec.split(',');for(const binding of bindings){const[attr,signalName]=binding.split(':');const signal=signals[signalName];if(signal){createEffect(()=>{const value=signal();if(value===false||value===null||value===undefined){attrEl.removeAttribute(attr);}else if(value===true){attrEl.setAttribute(attr,'');}else{attrEl.setAttribute(attr,String(value));}});}}}const eventTypes=['click','input','change','submit','keydown','keyup','keypress','focus','blur'];for(const eventType of eventTypes){const eventElements=el.querySelectorAll(`[data-pynext-${eventType}]`);for(const eventEl of eventElements){const handlerCode=eventEl.getAttribute(`data-pynext-${eventType}`);const handler=createHandler(handlerCode,signals,stores);eventEl.addEventListener(eventType,(e)=>{if(eventType==='submit'){e.preventDefault();}handler(e);});}}}function createHandler(code,signals,stores){const scopeNames=[...Object.keys(signals),...Object.keys(stores)];const scopeValues=[...Object.values(signals),...Object.values(stores)];try{const fn=new Function(...scopeNames,'e',code);return(e)=>fn(...scopeValues,e);}catch(err){console.error('[PyNext] Failed to create handler:',code,err);return()=>{};}}function normalizeNodes(content){if(content===null||content===undefined){return[];}if(Array.isArray(content)){return content.flatMap(normalizeNodes);}if(content instanceof Node){return[content];}return[document.createTextNode(String(content))];}if(typeof window!=='undefined'){window.PyNext={createSignal,createEffect,createMemo,createStore,batch,untrack,Show,For,Index,Switch,Match,Portal,ErrorBoundary,hydrate,hydrateIsland};}
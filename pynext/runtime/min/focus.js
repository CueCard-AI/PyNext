(function(global){'use strict';const focusStack=[];const activeFocusTraps=new Set();const rovingConfigs=new Map();function getFocusableElements(container){const selector=['a[href]:not([disabled])','button:not([disabled])','input:not([disabled])','select:not([disabled])','textarea:not([disabled])','[tabindex]:not([tabindex="-1"]):not([disabled])','[contenteditable="true"]',].join(', ');return Array.from(container.querySelectorAll(selector)).filter(el=>{const style=window.getComputedStyle(el);return style.display!=='none'&&style.visibility!=='hidden'&&el.offsetParent!==null;});}function createFocusTrap(container,options={}){const{autoFocus=true,restoreFocus=true}=options;if(restoreFocus&&document.activeElement){focusStack.push(document.activeElement);}function handleKeyDown(event){if(event.key!=='Tab')return;const focusable=getFocusableElements(container);if(focusable.length===0)return;const first=focusable[0];const last=focusable[focusable.length-1];if(event.shiftKey){if(document.activeElement===first){event.preventDefault();last.focus();}}else{if(document.activeElement===last){event.preventDefault();first.focus();}}}container.addEventListener('keydown',handleKeyDown);activeFocusTraps.add(container);if(autoFocus){const focusable=getFocusableElements(container);if(focusable.length>0){requestAnimationFrame(()=>focusable[0].focus());}}log(`Focus trap activated:${container.id||'anonymous'}`);return{release:()=>{container.removeEventListener('keydown',handleKeyDown);activeFocusTraps.delete(container);if(restoreFocus&&focusStack.length>0){const previousElement=focusStack.pop();if(previousElement&&document.body.contains(previousElement)){previousElement.focus();}}log(`Focus trap released:${container.id||'anonymous'}`);},focusFirst:()=>{const focusable=getFocusableElements(container);if(focusable.length>0)focusable[0].focus();},focusLast:()=>{const focusable=getFocusableElements(container);if(focusable.length>0)focusable[focusable.length-1].focus();},};}function initFocusTraps(){document.querySelectorAll('[data-pynext-focus-trap="true"]').forEach(container=>{const autoFocus=container.dataset.focusTrapAutofocus!=='false';createFocusTrap(container,{autoFocus,restoreFocus:true});});}function pushFocus(){if(document.activeElement&&document.activeElement!==document.body){focusStack.push(document.activeElement);log('Focus pushed to stack');}}function popFocus(){if(focusStack.length>0){const element=focusStack.pop();if(element&&document.body.contains(element)){element.focus();log('Focus restored from stack');}}}function useFocusRestore(){const previousElement=document.activeElement;return()=>{if(previousElement&&document.body.contains(previousElement)){previousElement.focus();}};}function createRovingFocus(container,options={}){const{selector='[data-roving-item]',orientation='vertical',loop=true,}=options;let currentIndex=0;function getItems(){return Array.from(container.querySelectorAll(selector));}function focusItem(index){const items=getItems();if(items.length===0)return;if(loop){index=(index+items.length)% items.length;}else{index=Math.max(0,Math.min(index,items.length-1));}items.forEach((item,i)=>{item.setAttribute('tabindex',i===index ? '0':'-1');});items[index].focus();currentIndex=index;}function handleKeyDown(event){const items=getItems();if(items.length===0)return;let handled=false;switch(event.key){case 'ArrowDown':if(orientation==='vertical'||orientation==='both'){focusItem(currentIndex+1);handled=true;}break;case 'ArrowUp':if(orientation==='vertical'||orientation==='both'){focusItem(currentIndex-1);handled=true;}break;case 'ArrowRight':if(orientation==='horizontal'||orientation==='both'){focusItem(currentIndex+1);handled=true;}break;case 'ArrowLeft':if(orientation==='horizontal'||orientation==='both'){focusItem(currentIndex-1);handled=true;}break;case 'Home':focusItem(0);handled=true;break;case 'End':focusItem(items.length-1);handled=true;break;}if(handled){event.preventDefault();}}container.addEventListener('keydown',handleKeyDown);const items=getItems();items.forEach((item,i)=>{item.setAttribute('tabindex',i===0 ? '0':'-1');});const id=container.id||`roving_${Date.now()}`;rovingConfigs.set(id,{container,options,cleanup:()=>{container.removeEventListener('keydown',handleKeyDown);rovingConfigs.delete(id);}});log(`Roving focus initialized:${id}(${orientation})`);return{focusItem,destroy:()=>{container.removeEventListener('keydown',handleKeyDown);rovingConfigs.delete(id);},};}function initRovingFocus(){document.querySelectorAll('[data-roving-group]').forEach(container=>{const orientation=container.dataset.rovingOrientation||'vertical';const loop=container.dataset.rovingLoop!=='false';const selector=container.dataset.rovingSelector||'[data-roving-item]';createRovingFocus(container,{orientation,loop,selector});});}function initSkipLinks(){document.querySelectorAll('[data-skip-link]').forEach(link=>{const targetId=link.dataset.skipLink;link.addEventListener('click',(event)=>{event.preventDefault();const target=document.getElementById(targetId);if(target){target.setAttribute('tabindex','-1');target.focus();target.removeAttribute('tabindex');}});});}function initFocusVisible(){let hadKeyboardEvent=false;document.addEventListener('keydown',(event)=>{if(event.key==='Tab'){hadKeyboardEvent=true;}});document.addEventListener('mousedown',()=>{hadKeyboardEvent=false;});document.addEventListener('focusin',(event)=>{if(hadKeyboardEvent){event.target.setAttribute('data-focus-visible','');}});document.addEventListener('focusout',(event)=>{event.target.removeAttribute('data-focus-visible');});}const DEBUG=typeof window!=='undefined'&&window.__PYNEXT_DEBUG__;function log(...args){if(DEBUG){}}function init(){initFocusTraps();initRovingFocus();initSkipLinks();initFocusVisible();log('Focus runtime initialized');}function refresh(){initFocusTraps();initRovingFocus();}if(typeof window!=='undefined'){window.__pynext__=window.__pynext__||{};window.__pynext__.focus={createFocusTrap,getFocusableElements,pushFocus,popFocus,useFocusRestore,createRovingFocus,refresh,};if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init);}else{init();}document.addEventListener('pynext:navigated',refresh);}if(typeof module!=='undefined'&&module.exports){module.exports={createFocusTrap,getFocusableElements,pushFocus,popFocus,useFocusRestore,createRovingFocus,refresh,};}})(typeof window!=='undefined' ? window:global);
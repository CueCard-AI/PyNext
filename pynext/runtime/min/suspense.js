(function(){'use strict';const SuspenseState={PENDING:'pending',RESOLVED:'resolved',FALLBACK:'fallback',TIMEOUT:'timeout'};const suspenseBoundaries=new Map();function createSuspense(id,options={}){const{pendingResources=[],timeout=null}=options;const boundary={id,state:pendingResources.length>0 ? SuspenseState.PENDING:SuspenseState.RESOLVED,pendingResources:new Set(pendingResources),timeout,element:null,timeoutId:null};suspenseBoundaries.set(id,boundary);boundary.element=document.querySelector(`[data-suspense="${id}"]`);if(timeout&&boundary.state===SuspenseState.PENDING){boundary.timeoutId=setTimeout(()=>{if(boundary.state===SuspenseState.PENDING){boundary.state=SuspenseState.TIMEOUT;updateBoundaryDOM(boundary);}},timeout);}if(pendingResources.length>0){watchResources(boundary);}return boundary;}function watchResources(boundary){const checkResources=()=>{const allResolved=[...boundary.pendingResources].every(resourceId=>{const resource=__pynext__.getResource?.(resourceId);return resource&&resource.state==='ready';});if(allResolved){resolveBoundary(boundary.id);}};checkResources();if(boundary.state===SuspenseState.PENDING){const pollId=setInterval(()=>{if(boundary.state!==SuspenseState.PENDING){clearInterval(pollId);return;}checkResources();},100);}}function resolveBoundary(id){const boundary=suspenseBoundaries.get(id);if(!boundary)return;if(boundary.timeoutId){clearTimeout(boundary.timeoutId);boundary.timeoutId=null;}boundary.state=SuspenseState.RESOLVED;updateBoundaryDOM(boundary);}function updateBoundaryDOM(boundary){if(!boundary.element){boundary.element=document.querySelector(`[data-suspense="${boundary.id}"]`);}if(boundary.element){boundary.element.setAttribute('data-state',boundary.state);if(boundary.state===SuspenseState.RESOLVED){const template=boundary.element.querySelector('[data-suspense-content]');const fallback=boundary.element.querySelector('[data-suspense-fallback]');if(template&&template.content){const content=template.content.cloneNode(true);boundary.element.innerHTML='';boundary.element.appendChild(content);if(__pynext__.hydrateElement){__pynext__.hydrateElement(boundary.element);}}else if(fallback){fallback.style.display='none';}}}}function replaceSuspense(id,html){const boundary=suspenseBoundaries.get(id);let element=boundary?.element;if(!element){element=document.querySelector(`[data-suspense="${id}"]`);}if(element){const temp=document.createElement('div');temp.innerHTML=html;while(temp.firstChild){element.parentNode.insertBefore(temp.firstChild,element);}element.remove();if(boundary){boundary.state=SuspenseState.RESOLVED;boundary.element=null;}if(__pynext__.hydrate){__pynext__.hydrate();}}}function getSuspense(id){return suspenseBoundaries.get(id);}function hasPendingSuspense(){for(const boundary of suspenseBoundaries.values()){if(boundary.state===SuspenseState.PENDING){return true;}}return false;}async function waitForSuspense(timeout=10000){const start=Date.now();while(hasPendingSuspense()){if(Date.now()-start>timeout){return false;}await new Promise(resolve=>setTimeout(resolve,50));}return true;}function setupShow(element,condition){const updateVisibility=()=>{const shouldShow=typeof condition==='function' ? condition():condition;const content=element.querySelector('[data-show-content]');const fallback=element.querySelector('[data-show-fallback]');if(content)content.style.display=shouldShow ? '':'none';if(fallback)fallback.style.display=shouldShow ? 'none':'';};updateVisibility();return updateVisibility;}function setupErrorBoundary(element,onError){const handleError=(error)=>{const fallback=element.querySelector('[data-error-fallback]');const content=element.querySelector('[data-error-content]');if(content)content.style.display='none';if(fallback){fallback.style.display='';const messageEl=fallback.querySelector('[data-error-message]');if(messageEl){messageEl.textContent=error.message||String(error);}}if(onError)onError(error);};window.addEventListener('error',(event)=>{if(element.contains(event.target)){handleError(event.error);}});return handleError;}if(typeof window!=='undefined'){window.__pynext__=window.__pynext__||{};window.__pynext__.SuspenseState=SuspenseState;window.__pynext__.createSuspense=createSuspense;window.__pynext__.resolveBoundary=resolveBoundary;window.__pynext__.replaceSuspense=replaceSuspense;window.__pynext__.getSuspense=getSuspense;window.__pynext__.hasPendingSuspense=hasPendingSuspense;window.__pynext__.waitForSuspense=waitForSuspense;window.__pynext__.suspenseBoundaries=suspenseBoundaries;window.__pynext__.setupShow=setupShow;window.__pynext__.setupErrorBoundary=setupErrorBoundary;}if(typeof module!=='undefined'&&module.exports){module.exports={SuspenseState,createSuspense,resolveBoundary,replaceSuspense,getSuspense,hasPendingSuspense,waitForSuspense,setupShow,setupErrorBoundary};}})();
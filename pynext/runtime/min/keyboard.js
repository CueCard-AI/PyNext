(function(global){'use strict';const isMac=typeof navigator!=='undefined'&&/Mac|iPod|iPhone|iPad/.test(navigator.platform);const shortcuts=new Map();const sequences=new Map();const handlers=new Map();let sequenceBuffer=[];let sequenceTimeout=null;const SEQUENCE_TIMEOUT=1000;function registerShortcut(config){shortcuts.set(config.id,config);log(`Registered shortcut:${formatShortcut(config)}`);}function registerSequence(config){sequences.set(config.id,config);log(`Registered sequence:${config.keys.join(' → ')}`);}function registerHandler(id,fn){handlers.set(id,fn);}function unregisterShortcut(id){shortcuts.delete(id);}function unregisterSequence(id){sequences.delete(id);}function matchesShortcut(event,shortcut){if(event.key.toLowerCase()!==shortcut.key){return false;}const modifiers=shortcut.modifiers||[];const needsMeta=modifiers.includes('meta');if(needsMeta!==event.metaKey)return false;const needsCtrl=modifiers.includes('ctrl');if(needsCtrl!==event.ctrlKey)return false;const needsAlt=modifiers.includes('alt');if(needsAlt!==event.altKey)return false;const needsShift=modifiers.includes('shift');if(needsShift!==event.shiftKey)return false;return true;}function checkContext(event,shortcut){const target=event.target;const isInput=target.matches('input, textarea, select, [contenteditable="true"]');const isInDialog=target.closest('[data-pynext-dialog][data-state="open"], [role="dialog"]');switch(shortcut.context){case 'global':if(isInput&&!isSpecialKey(shortcut.key)){return false;}return true;case 'input':return isInput;case 'dialog':return!!isInDialog;case 'always':return true;default:return!isInput;}}function isSpecialKey(key){return['escape','enter','tab'].includes(key.toLowerCase());}function processSequenceKey(key){if(sequenceTimeout){clearTimeout(sequenceTimeout);}sequenceBuffer.push(key.toLowerCase());sequenceTimeout=setTimeout(()=>{sequenceBuffer=[];},SEQUENCE_TIMEOUT);const currentSequence=sequenceBuffer.join(' ');for(const[id,sequence]of sequences){const targetSequence=sequence.keys.join(' ');if(currentSequence===targetSequence){sequenceBuffer=[];clearTimeout(sequenceTimeout);return sequence;}if(targetSequence.startsWith(currentSequence)){return null;}}sequenceBuffer=[];return null;}function handleKeyDown(event){if(event.isComposing)return;if(event.metaKey||event.ctrlKey||event.altKey){for(const[id,shortcut]of shortcuts){if(matchesShortcut(event,shortcut)&&checkContext(event,shortcut)){const handler=handlers.get(shortcut.handlerId);if(handler){if(shortcut.preventDefault){event.preventDefault();}try{handler(event);}catch(e){console.error(`Shortcut handler error(${id}):`,e);}return;}}}}if(!event.metaKey&&!event.ctrlKey&&!event.altKey){const target=event.target;const isInput=target.matches('input, textarea, select, [contenteditable="true"]');if(!isInput||event.key==='Escape'){const matchedSequence=processSequenceKey(event.key);if(matchedSequence){const handler=handlers.get(matchedSequence.handlerId);if(handler){event.preventDefault();try{handler(event);}catch(e){console.error(`Sequence handler error(${matchedSequence.id}):`,e);}}}}}for(const[id,shortcut]of shortcuts){if(!shortcut.modifiers||shortcut.modifiers.length===0){if(matchesShortcut(event,shortcut)&&checkContext(event,shortcut)){const handler=handlers.get(shortcut.handlerId);if(handler){if(shortcut.preventDefault){event.preventDefault();}try{handler(event);}catch(e){console.error(`Shortcut handler error(${id}):`,e);}return;}}}}}function formatShortcut(shortcut){const parts=[];if(shortcut.modifiers){if(shortcut.modifiers.includes('meta')){parts.push(isMac ? '⌘':'Ctrl');}if(shortcut.modifiers.includes('ctrl')){parts.push('Ctrl');}if(shortcut.modifiers.includes('alt')){parts.push(isMac ? '⌥':'Alt');}if(shortcut.modifiers.includes('shift')){parts.push('⇧');}}parts.push(shortcut.key.toUpperCase());return parts.join('+');}function getRegisteredShortcuts(){return Array.from(shortcuts.values()).map(s=>({combo:formatShortcut(s),id:s.id,context:s.context,}));}function getRegisteredSequences(){return Array.from(sequences.values()).map(s=>({sequence:s.keys.join(' → '),id:s.id,}));}const DEBUG=typeof window!=='undefined'&&window.__PYNEXT_DEBUG__;function log(...args){if(DEBUG){}}function init(){document.addEventListener('keydown',handleKeyDown);log('Keyboard runtime initialized');log('Platform:',isMac ? 'Mac':'Other');}function hydrate(data){if(data.shortcuts){data.shortcuts.forEach(registerShortcut);}if(data.sequences){data.sequences.forEach(registerSequence);}log(`Hydrated ${shortcuts.size}shortcuts,${sequences.size}sequences`);}if(typeof window!=='undefined'){window.__pynext__=window.__pynext__||{};window.__pynext__.keyboard={registerShortcut,registerSequence,registerHandler,unregisterShortcut,unregisterSequence,getRegisteredShortcuts,getRegisteredSequences,formatShortcut,hydrate,isMac,};if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init);}else{init();}}if(typeof module!=='undefined'&&module.exports){module.exports={registerShortcut,registerSequence,registerHandler,unregisterShortcut,unregisterSequence,getRegisteredShortcuts,getRegisteredSequences,formatShortcut,hydrate,};}})(typeof window!=='undefined' ? window:global);